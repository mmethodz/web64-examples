# Hybrid ASM Uses C Data

The Web64-C entry point calls a small assembly routine that reads a byte declared by C. This demonstrates C-to-assembly calls and assembly access to externally linked C storage without replacing the compiler-generated startup.

Open hybrid-asm-uses-c-data.web64proj in Web64 IDE.
