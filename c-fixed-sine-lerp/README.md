# C Fixed Sine Lerp

Web64 C fixed-point example that combines `web64_sin8`, `web64_fix8_mul`, and `web64_fix8_lerp` for a small animated value path.

Open `c-fixed-sine-lerp.web64proj` in Web64 IDE. The generated build output should include the fixed multiply and fixed trig runtime modules.
