# C Fixed Subpixel Scroll

Web64 C fixed-point example that accumulates 8.8 sprite position and fine scroll offsets.

Open `c-fixed-subpixel-scroll.web64proj` in Web64 IDE. The project uses `web64/fixed.h` macros and wrapping 8.8 addition without pulling fixed runtime modules.
