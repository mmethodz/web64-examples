# C Fixed Motion

Web64 C fixed-point motion-helper example using `Web64Motion2D` and `web64_motion2d_integrate`.

Open `c-fixed-motion.web64proj` in Web64 IDE. The generated build output should include the fixed vector runtime module and keep multiply, division, and trigonometry modules out of the closure.
